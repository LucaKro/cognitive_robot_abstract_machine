## The part
{{ part }}, labelled "{{ part_label }}"

{{ ontology }}

## What was measured
{{ measured }}

## The candidates
{% for candidate in candidates %}
{{ candidate.name }}: shares {{ candidate.shared_faces }} faces with it, touches it along {{ candidate.touching_edges }} edges, {{ candidate.distance }} m between their surfaces, and would hold it in its {{ candidate.field_name }}
{% endfor %}

## The picture
{{ painted }}
