## The ontology
{{ taxonomy }}

## The label
The label is "{{ label }}". The room carries {{ instances }} objects labelled with it.
The room's labels are: {{ every_label | join(", ") }}.

{{ meets }}

## The pictures
They show one of them, "{{ exemplar }}", chosen as the one whose faces are least shared with other labels.
